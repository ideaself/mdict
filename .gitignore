# Build artifacts
app/build/
.gradle/
build/
local.properties

# Release signing key (never commit)
keystore.properties

# Local dictionary files (keep on disk, not in git)
mydictionary/

# OS files
.DS_Store
Thumbs.db
*.log
